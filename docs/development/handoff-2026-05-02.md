# Clay Handoff - 2026-05-02

Дата восстановления: 2026-05-02
Проект: `/home/emma/Projects/clay/`
Источник planning-документов: `/home/emma/Documents/Obsidian/CachyOS/Trading/CLAY_Mission_Control/`
Локальная копия planning-документов: `/home/emma/Projects/clay/docs/mission-control/`
Текущая Codex-сессия:
`/home/emma/.codex/sessions/2026/05/02/rollout-2026-05-02T19-16-50-019de97a-7584-7dd0-a057-368e29d7657a.jsonl`

## Что Проверено

- Рабочая директория проекта существует: `/home/emma/Projects/clay/`.
- Репозиторий на ветке `main`, tracking `origin/main`.
- В репозитории уже есть `docs/planning/` с минимальным набором planning-документов.
- Полная папка `CLAY_Mission_Control` из Obsidian скопирована в `docs/mission-control/`.
- Указанный rollout-файл существует:
  `/home/emma/.codex/sessions/2026/04/15/rollout-2026-04-15T18-07-01-019d91ae-70c2-7b11-b392-2f9e8fe65d30.jsonl`
- Backend tests пройдены: `make backend-test` -> `101 passed`.
- Frontend tests пройдены: `make frontend-test` -> `11 passed`.
- Frontend production build пройден: `make frontend-build`.

## Где Остановились

Последняя активная development-сессия оборвалась 2026-04-30 после первого UI-pass в сторону Gemini `v17`.

По rollout-файлу последние подтвержденные действия:

- был проверен visual reference `/home/emma/Downloads/clay-mission-control-ui-v17`;
- принято решение не переносить mock app целиком, а интегрировать shell/navigation/composition в живой frontend;
- начат первый `v17` UI-pass в реальном `Clay`;
- изменены `App`, `AppSidebar`, `AppTopbar`, `StatusBadge`, frontend types;
- добавлены новые live screens `Overview` и `Settings`;
- обновлен `frontend/src/index.css` под более плотную terminal-грамматику;
- сессия завершилась из-за usage limit до финальной проверки и финального ответа.

После восстановления 2026-05-02 базовая build/test verification пройдена. UI-pass все еще стоит считать WIP с точки зрения визуальной приемки, но не с точки зрения очевидных TypeScript/test/build ошибок.

## Текущее Состояние Репозитория

Репозиторий работает на ветке `main`, tracking `origin/main`.

Закрепленные checkpoint-коммиты 2026-05-02:

- `7b6b9cc chore: checkpoint clay development state`
- `3369ad7 docs: add v17 ui reference export`
- `9d5842a ui: refine trading workspace layout`
- `9b88b0f ui: refine control center workspace`

Ключевое состояние после восстановления:

- Backend ingestion/freshness/incident lifecycle, reliability and control-center integration changes committed.
- Frontend shell/navigation aligned toward `v17`.
- New frontend screens committed:
  - `frontend/src/features/overview/overview-page.tsx`
  - `frontend/src/features/settings/settings-page.tsx`
- Frontend test script forces `NODE_ENV=test`, because the local shell had `NODE_ENV=production` and React test utilities loaded the production React build.
- Frontend accessibility/test contract aligned with the new `v17` navigation:
  - default screen is now `Overview`;
  - sidebar keeps `AI Console` visible while exposing `AI Control` as the nav accessible label;
  - sidebar keeps `Knowledge / Research` visible while exposing `Knowledge Base` as the nav accessible label;
  - Overview quick actions and TopBar session indicator have distinct accessible names.
- Frontend supports direct hash navigation for visual review, for example `/#workspace`, `/#control-center`, `/#settings`.
- 2026-05-02 second UI-pass started with `Trading Workspace`:
  - content no longer starts hidden behind an initial opacity animation;
  - workspace screen now uses a `v17`-style three-column terminal layout;
  - left rail owns active signals and monitoring pool;
  - center stack owns focused pair, situation map, and AI reasoning;
  - right stack owns risk, news/sentiment, and update meta.
- 2026-05-02 second UI-pass continued with `Control Center`:
  - page now uses a `v17`-style command header and tab strip;
  - `System Health` view has resource metrics, runtime control, ingestion guard, managed service cards, and runtime console;
  - `Audit Trail` view separates incidents and audit payloads;
  - `Active Configuration` view exposes config scopes and restore actions;
  - implementation keeps `useControlCenter()` and existing backend contracts.
- 2026-05-02 second UI-pass continued with `AI Console / AI Control`:
  - page now uses a `v17`-style command header and `Orchestration / Consensus / Terminal` views;
  - `Orchestration` owns model assignments, conflict/fallback posture, review card, and role matrix;
  - `Consensus` makes active conflicts, fallback state, and review queue visible;
  - `Terminal` provides a read-only orchestrator event view derived from the live snapshot;
  - implementation keeps `useAIControl()` and existing review/apply backend contracts.
- New documentation snapshot:
  - `docs/mission-control/`
  - `docs/development/`
  - `docs/README.md`

Не выполнять `git reset`, `git checkout --` или массовое удаление файлов без явной команды пользователя.

## Ближайший Безопасный План

1. В браузере проверить live routes:
   `http://127.0.0.1:5173/#overview`
   `http://127.0.0.1:5173/#workspace`
   `http://127.0.0.1:5173/#control-center`

2. Продолжать второй UI-pass по внутренним экранам:
   `Session Control`, `Session Review`, `Knowledge / Research`, `Reliability`, `Demo Validation`, `Validation Lab`.

3. После следующего UI-pass снова прогнать:
   `make frontend-test`
   `make frontend-build`
   `make backend-test`

4. После чистой проверки сделать следующий commit/push checkpoint.

## UI Reference

Текущий локальный visual reference:

`/home/emma/Projects/clay/docs/ui-references/clay-mission-control-ui-v17`

Использовать его как визуальный эталон, но не копировать mock data или mock-only architecture в живое приложение без адаптации к текущим hooks/API.

## Практическая Рамка

Сейчас главная задача не добавлять новые возможности, а визуально принять и затем аккуратно продолжить уже начатый `v17` pass:

- сохранить live hooks/API;
- сохранить реальные backend contracts;
- сохранить зеленые проверки;
- зафиксировать следующий чистый checkpoint.
